const styles = () => (
  {
    container: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center'
    },
    stepper: {
      maxWidth: 400,
      flexGrow: 1,
    }
  }
);

export default styles;